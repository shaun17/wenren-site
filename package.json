{
  "name": "pagecomet",
  "version": "1.0.0",
  "private": true,
  "description": "PageComet：以 Notion 为内容源、由 Astro 静态生成并部署到 Cloudflare Pages 的开源个人网站模板。",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "clean:notion-assets": "node scripts/clean-notion-assets.mjs",
    "validate:site-config": "node scripts/validate-site-config.mjs",
    "predev": "npm run clean:notion-assets && npm run validate:site-config",
    "dev": "cross-env CONTENT_SOURCE=notion astro dev",
    "prepare:fixture-media": "node scripts/prepare-media-fixtures.mjs",
    "predev:fixture": "npm run clean:notion-assets && npm run prepare:fixture-media",
    "dev:fixture": "cross-env CONTENT_SOURCE=fixture SITE_CONFIG_MODE=example astro dev",
    "check": "astro check",
    "prebuild": "npm run clean:notion-assets && npm run validate:site-config",
    "build": "cross-env CONTENT_SOURCE=notion astro build",
    "prebuild:fixture": "npm run clean:notion-assets && npm run prepare:fixture-media",
    "build:fixture": "cross-env CONTENT_SOURCE=fixture SITE_CONFIG_MODE=example astro build",
    "preview": "astro preview",
    "pages:dev": "npm run build && wrangler pages dev dist",
    "cloudflare:login": "wrangler login",
    "pages:create": "node --env-file-if-exists=.env scripts/create-pages-project.mjs",
    "verify:dist": "node scripts/verify-static-output.mjs",
    "test": "npm run check && npm run build:fixture && cross-env SITE_CONFIG_MODE=example node --import=tsx --test tests/*.test.mjs && npm run verify:dist",
    "deploy": "node --env-file-if-exists=.env scripts/deploy.mjs"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^26.1.1",
    "@types/three": "^0.185.1",
    "cross-env": "^10.1.0",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3",
    "wrangler": "4.113.0"
  },
  "dependencies": {
    "astro": "^7.1.3",
    "image-size": "^2.0.2",
    "linkpeek": "2.1.1",
    "three": "^0.185.1",
    "undici": "7.28.0"
  },
  "overrides": {
    "fast-uri": "3.1.4",
    "sharp": "0.35.3"
  }
}
